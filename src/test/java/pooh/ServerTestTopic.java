package pooh;

import org.junit.Test;

import static org.junit.Assert.assertEquals;

/**
 * ServerTest1 and ServerTestTopic must run independently. On each test one server thread!!!
 */
public class ServerTestTopic {


}